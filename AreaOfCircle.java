class AreaOfCircle {
    public static void main(String args[]) {
        int r = 3;
        double res = 3.14 * r * r; // Correct formula
        System.out.println("Area of the circle: " + res);
    }
}
// the code does not contain any loops or recursive calls, it executes a fixed number of operations regardless of the input. That's why the time complexity is 
// 𝑂(1)